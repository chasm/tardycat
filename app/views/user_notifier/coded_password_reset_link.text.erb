We received a request to reset your ToddyCat credentials. To do so, please follow the coded link below:

<%= password_reset_url(@user.code) %>

If you received this email in error, simply do nothing. The above link will expire at <%= @user.expires_at.to_formatted_s(:mail_timestamp) %>.

Thanks!
ToddyCat
